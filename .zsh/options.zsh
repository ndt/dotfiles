# Optionen für die History
setopt append_history hist_ignore_all_dups hist_ignore_space

# Optionen: Empfehlung aus dem Buch "ZSH" (OpenSourcePress)
setopt no_beep auto_cd complete_in_word correct extendedglob rm_star_wait

# Option: Safer Piping (>! bzw. >>!)
setopt no_clobber
